CREATE VIEW Favorite AS
SELECT Anime.*,COUNT(*) AS unseen_episodes
FROM Anime
JOIN Episode ON Anime._id = Episode.anime_id
WHERE Anime.favorite = 1 AND Episode.seen = 0
GROUP BY Anime._id;

getFavorites:
SELECT * FROM Favorite;
