<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Tadami" parent="Theme.Material3.DayNight.NoActionBar">

    </style>


    <!-- Cast button style -->
    <!--<style name="BaseCastTheme" parent="Theme.MediaRouter">
        <item name="mediaRouteButtonStyle">@style/BaseMediaRouteButtonStyle</item>
    </style>

    <style name="BaseMediaRouteButtonStyle" parent="Widget.MediaRouter.Light.MediaRouteButton">
        <item name="mediaRouteButtonTint">?attr/buttonTint</item>
    </style>-->

    <!-- SplashScreen Theme -->
    <style name="Theme.Tadami.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_tada_splash</item>
        <item name="windowSplashScreenBackground">#202125</item>
        <item name="postSplashScreenTheme">@style/Theme.Tadami</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" >false</item>
    </style>

</resources>